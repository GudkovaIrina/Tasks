
@{
    ViewBag.Title = "Пользователи и награды";
}

<h2>Добро пожаловать!</h2>

@Html.ActionLink("Пользователи", "Index", "User", null, new {@class = "btn btn-default"})
@if (User.Identity.IsAuthenticated && User.IsInRole("admin"))
{
    @Html.ActionLink("Награды", "Index", "Award", null, new { @class = "btn btn-default" })
}

